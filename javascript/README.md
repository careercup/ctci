####Run the code and test
This project uses Mocha and Chai for testing:

#####Install dependencies:

```bash
npm install -g mocha
npm install -g chai

```

#####Run tests:

```bash
mocha --recursive
```
